# /handoff

End-of-session summaries so the next session (a continuation, tomorrow's restart, or a different person/agent) can reconstruct context fast. One file per session: `<short-topic>-<YYYY-MM-DD>.md`. Generate with `/handoff`. See `CLAUDE.md` for the rules. This folder starts empty — the first real session fills it.
