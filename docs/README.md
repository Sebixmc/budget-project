# /docs

Long-lived project knowledge — the durable "how it works" that outlives any single session. Architecture, conventions, data flow. See `CLAUDE.md` in this folder for the authoring rules (short, dated, cross-linked).

For this project, the always-current picture and the work queue live in [`/spec.md`](../spec.md); `/docs/` is the stable reference distilled from it. Hard-to-reverse decisions go in [`/adr/`](../adr/); session logs go in [`/handoff/`](../handoff/).

Starting points: [`architecture.md`](architecture.md), [`conventions.md`](conventions.md), [`vision.md`](vision.md).
